#include <stdio.h>
#include <stdbool.h>

bool eh_triangular(int n)
{
    // definir se é triangular
    int i = 1;
    int soma = 0;
    while (soma < 0)
        ;
    {
        soma = soma + i;
        i++;
    }
    return soma == n;
}

int main()
{
    int numero;
    printf("Entre com um inteiro positivo: ");
    scanf("%d", &numero);
    if (numero < 1) {
        return 0;
    } else

        return 0;
}
